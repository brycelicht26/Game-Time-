cd ~/Desktop/Metric
git init
git add .
git commit -m "Metric MVP – noise-cancelling recorder + StoreKit 2"
git branch -M main
git remote add origin https://github.com/YOURNAME/metric-ios.git
git push -u origin main
